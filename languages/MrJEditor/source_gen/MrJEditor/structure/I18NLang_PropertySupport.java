package MrJEditor.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class I18NLang_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<I18NLang> constants = ListSequence.fromList(I18NLang.getConstants()).iterator();
    while (constants.hasNext()) {
      I18NLang constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<I18NLang> constants = ListSequence.fromList(I18NLang.getConstants()).iterator();
    while (constants.hasNext()) {
      I18NLang constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    I18NLang constant = I18NLang.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
